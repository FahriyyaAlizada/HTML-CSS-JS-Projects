// function logGreeting() {
//     console.log(`${name} is a JS developer`);
// }
// let name = "Faxriyya Alizada"
// logGreeting()

function name(userName) {
    console.log(`${userName} is active now...`);
}
name(prompt())
name(prompt())
name(prompt())